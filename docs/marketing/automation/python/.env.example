# Unit marketing automation — credentials template
# Copy to .env (gitignored) and fill in. Never commit the populated .env.

# === Apify (Instagram account studies — ig_account_fetch.py) ===
# Sign up free at https://apify.com ($5/mo credit renews, no card).
# Console → Settings → API & Integrations → copy token.
APIFY_TOKEN=
